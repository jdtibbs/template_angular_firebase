{
  "rules": {
    "profile": {
      ".read": "auth.uid !== null",
      ".write": "auth.uid !== null",
      "$profile_id": {
        ".validate": "newData.hasChildren(['firstName', 'lastName'])",
        "firstName": {
          ".validate": "newData.isString() && newData.val().length <= 50"
        },
        "lastName": {
          ".validate": "newData.isString() && newData.val().length <= 50"
        },
        "$other": {
          ".validate": false
        }
      }
    }
  }
}
